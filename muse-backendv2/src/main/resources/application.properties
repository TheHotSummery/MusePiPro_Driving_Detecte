# 应用服务 WEB 访问端口
server.port=8080
# Actuator Web 访问端口
management.server.port=8081
management.endpoints.jmx.exposure.include=*
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# 本地开发环境配置
# 使用MySQL数据库（V2版本）

# 数据库配置 - V2版本MySQL数据库
spring.datasource.url=jdbc:mysql://192.168.110.79:3306/muse_v2?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
spring.datasource.username=muse_v2
spring.datasource.password=NkpzGb4djWpREDYX
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置 - V2版本
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 禁用Redis配置（本地开发环境）
# 注意：不能禁用SecurityAutoConfiguration，因为Actuator需要它
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

# 日志配置
logging.level.com.spacemit.musebackendv2=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# 错误处理配置 - 不显示详细错误信息（避免泄露敏感信息）
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false
server.error.whitelabel.enabled=false

# CORS配置
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# JWT配置（本地开发）
jwt.secret=local-development-secret-key
jwt.expiration=86400000

# 高德地图API配置
# 注意：key是REST API的密钥，secret是Web端JS API的安全密钥
amap.api.key=*************
amap.api.secret=*************
amap.api.base-url=https://restapi.amap.com/v3

# 静态资源配置
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/**
spring.web.resources.add-mappings=true

